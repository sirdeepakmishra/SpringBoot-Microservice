server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
       server:
         git:
           uri: https://github.com/sirdeepakmishra/SpringBoot-Microservice
           clone-on-start: true